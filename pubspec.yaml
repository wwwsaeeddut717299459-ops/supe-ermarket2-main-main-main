name: almajed_pro

description: "almajedPRO Offline Business Management System"
publish_to: 'none'

version: 2.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  flutter_riverpod: ^2.6.1

  go_router: ^16.2.0

  drift: ^2.34.3
  sqlite3_flutter_libs: ^0.5.39

  path_provider: ^2.1.5
  file_picker: ^8.1.7
  path: ^1.9.1

  intl: ^0.20.2
  crypto: ^3.0.7
  archive: ^3.6.1
  pdf: ^3.11.3
  printing: ^5.14.2
  excel: ^4.0.6
  file_selector: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  build_runner: ^2.16.0
  drift_dev: ^2.34.5

flutter:
  uses-material-design: true
  assets:
    - assets/fonts/
    - assets/images/almajed.png